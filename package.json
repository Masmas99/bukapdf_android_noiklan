{
  "name": "docuview-android",
  "version": "1.0.0",
  "private": true,
  "description": "DocuView PDF reader Android app",
  "scripts": {
    "build": "npm run sync",
    "sync": "npx cap sync android",
    "android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.4",
    "@capacitor/core": "^7.4.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.4"
  }
}
